// Declaração de uma função:
// Ou Function statement

function createPhrases() {
  console.log('study is so good')
  console.log('pacient and persistence')
  console.log('review is the mother of learning')
}
// até aqui a função não é executada.
//Para executar a função devemos invoca-la:

createPhrases()

//agora se queremso terminar de rodar, poderemos fazer assim:

console.log('fim do programa')

//OBS: as "functions" servem para agruparem os códigos e dar significado a eles também, para que possamos usa-los quantas vezes quisermos apenas pela referencia. 